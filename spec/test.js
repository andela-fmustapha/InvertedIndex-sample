let jasmine = require('jasmine');
let expect = jasmine.expect;
let myApp = require('../src/inverted.js');

describe('Inverted Index Readfile', function () {
    it('reads uploaded files from front end', function () {
        expect(1 + 2).toEqual(3);
    });
});

describe('Inverted Index Class', function () {
    it('defines inverted index class', function () {
        expect(1 + 2).toEqual(3);
    });
});

describe('Inverted Index validate file', function () {
    it('validates each document in a file', function () {
        expect(1 + 2).toEqual(3);
    });
});

describe('Inverted Index tokenize', function () {
    it('', function () {
        expect(1 + 2).toEqual(3);
    });
});

describe('Inverted Index Readfile', function () {
    it('Read uploaded files from front end', function () {
        expect(1 + 2).toEqual(3);
    });
});